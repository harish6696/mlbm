decoder:
  out_features: ${data.num_channels}
  layers: 1
  layer_size: 128

fno:
  in_channels: ${data.num_channels}
  dimension: 2
  latent_channels: 32
  fno_layers: 5
  fno_modes: 100
  padding: 9